$('html').addClass('js');$('li').on('click', function() {	$('.menu li').removeClass('clicked');	$(this).addClass('clicked');});$(document).ready(function() {	var i;	for (i = 0; i < $('a').length; i++)		if ($('a')[i].href.substring(0, 7) == 'mailto:')			$('<sup class=sobaka>@</sup>').insertAfter($('a')[i]);})$('a').on('click', function(e) {	if (!($(this).attr('href').substring(0, 7) == 'mailto:')) {		e.preventDefault();		var href = $(this).attr('href');		if (!($(document).find('.sms p').length)) {			$('<div class=sms></div>').insertAfter('p:last')			$('<h1>Да</h1><h1>Нет</h1><p></p>').appendTo('.sms');		};		$('.sms p').text('Вы действительно хотите перейти по адресу ');		$('<b>' + href + '</b>').appendTo('.sms p');		$('<text>?</text>').insertAfter('.sms b')		$('.sms')['fadeIn'](spd);		$('.hd')['fadeIn'](spd);		$('.sms h1').on('click', function() {			if ($(this).text() == 'Да')				window.open(href, 'blank');			$('.sms')['fadeOut'](spd);			$('.hd')['fadeOut'](spd);		});		var top = (($(window).innerHeight() - $(this).height() - $('footer').outerHeight())/3);		$('.sms').css('top', top);	};});$('.hd').on('click', function() {	$('.sms')['fadeOut'](spd);	$(msg)['slideUp'](spd);	$('.hd')['fadeOut'](spd);});var CF = {	msg: $('#msg'),	init: function() {		$('<button></button>', {			text: 'Contact Me'		})			.insertAfter('p:last')			.on('click', this.show);	},		show: function() {		if (CF.msg.is(':hidden') ) {			CF.close.call(msg);			CF.msg[CF.style.effect2](spd);			$('.hd')[CF.style.effect](spd);		};	},	close: function() {		var $this = $(this);		if ($this.find('span.close').length) return;		$('<span class=close>X</span>')			.prependTo(this)			.on('click', function() {				$this[CF.style.effect2](spd);				$('.hd')[CF.style.effect](spd);			});	},	style: {		effect: 'fadeToggle',		effect2: 'slideToggle',		speed: 300	}};var spd = CF.style.speed;CF.init();