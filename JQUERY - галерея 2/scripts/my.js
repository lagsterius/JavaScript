$('.slider_nav').css('display', 'block');$('.slider').css('overflow', 'hidden');var time = 2000,	www = $(document).width()-50;	slider = new Slider($('.slider ul'), $('.slider_nav'));$('body').keydown(function(e) {	if (e.keyCode === 39) {		e.preventDefault();		slider.move('next', time);	}	else if (e.keyCode === 37) {		e.preventDefault();		slider.move('prev', time);	};	});$(document).ready(function() {	$('body').css('width', www);	$('.slider li img').css('width', www);	$('.slider ul').css('width', $('.slider ul').find('img').length * $('.slider ul').find('img').width());});function Slider(container, nav) {	this.container = container;	this.nav = nav;	this.imgs = this.container.find('img');	this.imgW = www;	this.imgsL = this.imgs.length;	this.cur = 0;	this.move = function(dir, time) {		this.cur += (~~(dir === 'next') || -1);		this.cur = (this.cur < 0) ? this.imgsL - 1 : this.cur % this.imgsL;		container.animate( {'margin-left': -(this.cur * this.imgW) }, time);	};}slider.nav.find('button').click(function() {	slider.move($(this).data('dir'), time);});